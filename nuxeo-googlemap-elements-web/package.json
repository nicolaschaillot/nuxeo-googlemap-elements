{
  "name": "nuxeo-googlemap-elements",
  "private": true,
  "devDependencies": {
    "cpr": "^2.0.2",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "clean": "rimraf build target",
    "prebuild": "npm run clean",
    "build": "mkdirp target/classes/nuxeo.war/ui/ && cpr bower_components/ target/classes/nuxeo.war/ui/bower_components -o && cpr nuxeo/ target/classes/ -o"
  }
}
